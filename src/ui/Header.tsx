function Header() {
    return (
        <header>
            <h2>FDA Drug Lookup</h2>
        </header>
    );
}

export default Header;
// This file defines a functional component named `Header` that renders a header section for a web application.